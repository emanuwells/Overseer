# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual env
.venv/
venv/

# OS / IDE / adaptadores legados na raiz
.agents/
.claude/
/templates/
*.egg-info/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
.codex_tmp/

# Runtime artifacts
logs/
# Pipelines externos vivem fora do repo; ignorar mounts locais acidentais na raiz
/pipelines/
_pipelines_removed/
pipelines/**/logs/
*.tgz
overseer_deploy.tgz
*.log
orch_log.txt
run_log.txt
runtime/logs/*.log
runtime/*.json
runtime/*.db
runtime/*_status.json
runtime/scheduler_state.json
runtime/run_now_channel/**/*.json
runtime/triggers/schedule/**/*.json
runtime/.retention_last_purge
runtime/overseer-py/**
runtime/runners/**
!runtime/.gitkeep
!runtime/logs/.gitkeep
!runtime/triggers/.gitkeep
!runtime/overseer-py/.gitkeep
!runtime/runners/.gitkeep

# Frontend generated payload
frontend/node_modules/
frontend/dist/
frontend/dist-nginx/
frontend/pm_payload.json
frontend/pm_details.json
frontend/public/overseer-config.js

# Environment local (secrets/.env; raiz legada ignorada)
.env
secrets/.env
deploy/runners.local/

# Secrets (keep only examples/tracked placeholders)
secrets/*
!secrets/*.example
!secrets/*.example.json
!secrets/.gitkeep
!secrets/README.md
secrets/ssh_key*

# Pipeline secrets (keep only examples/placeholders)
pipelines/**/secrets/*
!pipelines/**/secrets/.gitkeep
!pipelines/**/secrets/*.example
!pipelines/**/secrets/*.example.json
pipelines/**/secrets/ssh_key*
