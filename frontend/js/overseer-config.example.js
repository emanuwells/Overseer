// Copiar para overseer-config.js no deploy (gerado automaticamente no servidor).
// Em desenvolvimento local, deixar apiToken vazio se OVERSEER_API_TOKEN não estiver definido na API.
window.OVERSEER_CONFIG = {
  apiToken: '',
};
